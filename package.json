{
  "name": "@ghom/event-emitter",
  "version": "1.0.3",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "TypeScript only package for emit events (Node EventEmitter-like)",
  "prettier": {
    "semi": false
  },
  "scripts": {
    "format": "prettier --write src tsconfig.*",
    "test": "tsc --noEmit",
    "prepublishOnly": "npm run format && npm test"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "typescript": "^4.5.5"
  }
}
